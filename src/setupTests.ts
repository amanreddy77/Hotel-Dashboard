// src/setupTests.ts
import '@testing-library/jest-dom'; // for additional matchers
import { expect } from 'vitest'; // Use Vitest's expect

// If you need to add any custom matchers or setups, you can do it here.
